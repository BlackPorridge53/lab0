# Lab0 实验报告：GitLab

## 一、文档问题回答

### 1. 你之前有过多人协同开发的经历吗？如果有，你们是使用什么方式分工协作的？

有，通过 GitHub 分支协作，各自开发后合并代码。

### 2. Git 为什么要设计“暂存—提交”两个步骤？

暂存可筛选本次修改，使每次提交内容清晰独立。

### 3. `git branch` 和 `git branch -a` 的区别是什么？

`git branch` 查看本地分支；`git branch -a` 还显示远程跟踪分支。

### 4. Git 相关文章阅读

我选择阅读 **Commit Message 规范** 和 **语义化版本**（尽管在本次实验中，由于不明确代码功能而并没有完全遵守commit message的规范）。

#### 1）Commit Message 规范

文章主要介绍了 Git 提交信息的规范写法。规范的 Commit Message 能清楚记录一次修改的目的，并可使用 `feat`、`fix`、`docs` 等类型区分不同修改，还能够为生成 Change Log 提供基础。这说明 commit 不只是保存代码，也是在记录项目的发展过程。

#### 2）语义化版本

语义化版本使用“主版本号.次版本号.修订号”的形式管理软件版本。不兼容的 API 修改增加主版本号；向下兼容的新功能增加次版本号；向下兼容的问题修正增加修订号。通过统一规则，开发者看到版本号就可以大致了解软件发生了什么程度的变化。

#### 3）为什么要学习 Git？

我认为 Git 更重要的价值是让软件开发中的变化变得可记录、可追踪、可协作。规范的提交信息和版本号又进一步使这些变化能够被其他开发者理解，这对于长期维护和多人协作都十分重要。

## 二、实验步骤

### 1. .创建 `feature` 分支

创建后没有立即切换到该分支，而是在`main`中直接对`main.c`进行修改，完成了任务二的同时，可以制造任务四要求的合并冲突。再切换到`feature`分支，对`main.c`进行了不同修改。

### 2. 合并并产生冲突

在 `main` 分支执行合并，由于 `main` 和 `feature` 分支从共同版本分开后，都修改了 `main.c` 的同一部分，Git 无法自动判断最终应该保留一份内容，因此出现了合并冲突：

![1789665535444](image/lab0/1789665535444.png)

### 3. 解决冲突

打开 `main.c` 后，可以看到 Git 插入的冲突标记：

```text
<<<<<<< HEAD
	printf("Hello, ics!\n");
=======
	printf("qwq\n");
>>>>>>> feature
```

我最终保留feature的修改，并删除 `<<<<<<<`、`=======`、`>>>>>>>` 等冲突标记。随后将解决后的文件重新加入暂存区并提交。

至此，`feature` 分支的内容成功合并到 `main` 分支，冲突处理完成。

## 四、实验建议

也许可以在分支合并部分增加提交历史示意图的小任务，帮助理解分支为何产生冲突，何版本产生了冲突。
